#!/usr/bin/env perl

use strict;
use warnings;

sub previews {
    my ($input) = @_;
    print "You entered: $input\n";
}

sub list {
    my ($input) = @_;
    previews($input);
}

sub names {
    my ($input) = @_;
    print "voce entrou: $input\n";
}

sub views {
    my ($input) = @_;
    print "voce entrou: $input\n";
}